# ── Core (required; proven to run) ──
numpy
pyyaml

# ── Global NER detection (recommended for full coverage) ──
# Names, addresses, orgs, medical conditions need NER, not regex.
presidio-analyzer
presidio-anonymizer
# then: python -m spacy download en_core_web_lg

# ── Real embeddings (recommended for production retrieval) ──
sentence-transformers

# PDF text extraction
PyPDF2
pdfplumber

# ── Vector store connectors (install ONLY the one you use) ──
# Pinecone client
pinecone-client
# qdrant-client
# psycopg[binary]      # for pgvector
# faiss-cpu            # for local FAISS
rank_bm25            # optional hybrid lexical scoring

# Web framework and server
fastapi
uvicorn[standard]
python-multipart

# dotenv for environment loading
python-dotenv
openai
rank_bm25
gunicorn
python-docx
pandas
openpyxl
